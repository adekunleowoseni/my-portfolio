/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'velonpay.com',
            'project2.com',
            'project3.com'
            // Add other domains as needed
        ],
    },
}

module.exports = nextConfig 